extends ../layout

block content
    div.columns
        form(action='/admin/tournament/' + games[0].tournament_id + '/results' method="post" class="form-horizontal")
            table.table.table-horizontal
                thead
                    th PVM
                    th Kotijoukkue
                    th Maalit
                    th -
                    th Vierasjoukkue
                    th Maalit
                    th Tulos
                tbody
                    div.form-group
                        for game in games
                            tr
                                td= moment(game.game_start_datetime).format('D.M.YYYY H:mm')
                                td
                                    input(type='number' name="goals_1" value=game.team_1_score || null)
                                td= game.team_1
                                td -
                                td= game.team_2
                                td
                                    input(type='number' name="goals_2" value=game.team_2_score || null)
                                td= game.result
                                input(type='hidden' value=game.id name='game_id')
            div.form-group.pt-1-4
                div.col-9
                div.col-1
                    a(class='btn btn-link' href="/admin/tournamentManagement") Peruuta
                div.col-2
                    button(class='btn btn-primary' type='submit') Tallenna tulokset